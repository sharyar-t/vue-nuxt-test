{
  "name": "vue2-nuxt3-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start:admin\" \"npm run start:client\" \"npm run start:db\"",
    "start:admin": "cd admin && npm install && npm run serve",
    "start:client": "cd client && npm install && npm run dev",
    "start:db": "json-server --watch db.json --port 4000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
